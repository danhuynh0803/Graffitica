#ifndef VERTEX_H
#define VERTEX_H

#include "math/vector.h"

/*
struct vertex 
{
    vertex(const vec3f &pos, const vec4f &col) : 
        position(pos), color(col) {}

    // Vertex attributes
    vec3f position; 
    vec4f color;
};
*/
#endif // VERTEX_H
